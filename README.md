# دفتر العناوين — Jawwal Address Book

## 🚀 تشغيل المشروع

### المتطلب الوحيد
- [Node.js](https://nodejs.org/) — حمّل النسخة LTS

### الخطوات (3 فقط)

```bash
npm install
npm run dev
```

افتح المتصفح على: **http://localhost:5173**

---

> **ملاحظة:** `npm install` مع Vite أسرع بكثير من react-scripts
> لأنه يحمّل ~30 مكتبة بدل ~1500 مكتبة.

## 📁 الملفات

```
address-book-app/
├── index.html               ← صفحة HTML (Tailwind من CDN)
├── vite.config.js           ← إعداد Vite
├── package.json
└── src/
    ├── main.jsx             ← نقطة الدخول
    └── AddressBookApp.jsx   ← كامل التطبيق
```
